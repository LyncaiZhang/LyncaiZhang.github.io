# 个人学术主页维护说明书

本项目是一个静态 GitHub Pages 个人学术主页，不需要前端构建工具，也不需要安装依赖。日常维护可以理解为三件事：

1. 文字内容大多改 `script.js`。
2. 样式、排版、动效大多改 `styles.css`。
3. 头像、二维码、论文 PDF、论文配图放在 `assets/` 目录。

## 一、文件结构

```text
D:\codex_page
├─ index.html                 页面结构、导航锚点、资源引用
├─ script.js                  中英双语内容、时间线、动态、研究兴趣、论文、札记、动效逻辑
├─ styles.css                 页面视觉样式、响应式布局、头像、侧栏、动态背景样式
├─ assets/
│  ├─ avatar.jpg              主页头像
│  ├─ wechat-qrcode.jpg       微信公众号二维码
│  ├─ papers/                 论文 PDF
│  └─ paper-thumbs/           论文配图
└─ 维护说明书.md              本说明书
```

## 二、本地预览

每次修改后，建议先本地预览，再提交到 GitHub。

```powershell
cd D:\codex_page
python -m http.server 8765
```

浏览器打开：

```text
http://127.0.0.1:8765/index.html
```

如果页面没有刷新，按 `Ctrl + F5` 强制刷新。

## 三、最常见材料如何修改

### 1. 修改姓名、学校、简介

打开 `script.js`，修改：

```js
content.zh.profile
content.en.profile
content.zh.hero
content.en.hero
```

中文页面改 `zh`，英文页面改 `en`。如果只改中文，不改英文，切换语言后会出现旧内容。

### 2. 修改邮箱

当前邮箱是：

```text
jamelincoln1012@gmail.com
```

如需替换，建议在全项目搜索这个邮箱，并同步替换 `index.html` 和 `script.js` 中的所有出现位置。重点包括：

```js
content.zh.contact.email
content.en.contact.email
const emailAddress
```

当前邮箱按钮采用“站内展示 + 复制邮箱”的方式，不依赖本机邮件客户端。

### 3. 替换头像

直接替换这个文件：

```text
assets/avatar.jpg
```

建议使用清晰的竖版证件照或形象照。替换后如果头像裁切不理想，打开 `styles.css` 调整：

```css
.avatar {
  width: 146px;
  height: 166px;
}

.avatar img {
  object-position: 42% 50%;
}
```

如果线上仍显示旧头像，可能是浏览器缓存。可以先强制刷新；必要时在 `index.html` 中把头像地址改成：

```html
assets/avatar.jpg?v=20260619
```

日期数字可以换成当天日期。

### 4. 替换微信公众号二维码

直接替换：

```text
assets/wechat-qrcode.jpg
```

保持文件名不变，页面会自动使用新二维码。

### 5. 添加或修改 News 动态

打开 `script.js`，修改：

```js
data.zh.news
data.en.news
```

格式如下：

```js
{
  date: "2026.06",
  text: "这里写一条中文动态。",
}
```

英文版也要同步添加：

```js
{
  date: "Jun 2026",
  text: "Write the English update here.",
}
```

建议把最新动态放在数组最上方。主页默认只展示前 3 条，点击“查看更多”后展示全部。

### 6. 添加或修改时间线

打开 `script.js`，修改：

```js
data.zh.status
data.en.status
```

每条经历格式如下：

```js
{
  label: "2024.09 - Present",
  title: "同济大学知识产权学博士研究生",
  body: "这里写经历说明。",
}
```

如果是英文页面，对应写在 `data.en.status`。

### 7. 修改研究兴趣

打开 `script.js`，修改：

```js
data.zh.topics
data.en.topics
```

每个研究兴趣由标题和说明组成：

```js
{
  title: "明星科学家团队与科技成果转化效能",
  body: "这里写 2-3 句话，说明研究问题、方法和合作方向。",
}
```

目前主页建议维持 3 个研究兴趣，信息最清爽。

### 8. 添加 Working Paper

打开 `script.js`，修改：

```js
data.zh.workingPapers
data.en.workingPapers
```

当前是标题列表：

```js
workingPapers: [
  "研发要素跨境流动政策供给分析",
  "全球高校专利转化效能提升的内部保障",
],
```

英文也要同步：

```js
workingPapers: [
  "Policy Supply Analysis of Cross-Border Flows of R&D Factors",
  "Internal Safeguards for Improving Patent Commercialization Performance in Global Universities",
],
```

后续如果需要给 Working Paper 添加摘要、会议版本或项目链接，可以再把这一块升级为卡片式结构。

### 9. 添加论文或成果

论文数据主要维护在：

```js
data.zh.publications
```

英文论文列表目前会根据中文论文列表自动生成，但每篇论文仍建议补充英文题名和英文贡献说明。

新增论文的基本格式：

```js
{
  title: "中文题名",
  cnTitle: "English Title",
  authors: "作者1, 作者2, 张凌恺",
  venue: "期刊名称, 2026",
  type: "journal",
  summary: "中文贡献说明，一句话说明研究问题、方法或发现。",
  enSummary: "English contribution statement.",
  links: [
    { label: "PDF", kind: "paper", href: "assets/papers/example-paper.pdf" },
    { label: "Code", kind: "code", href: "" },
    { label: "Project", kind: "project", href: "" },
  ],
}
```

`type` 常用值：

```text
featured    代表作
journal     期刊论文
thesis      学位论文
```

没有代码或项目页时，可以先不写对应链接，或者保留空链接后续再补。

### 10. 添加论文 PDF

把 PDF 放入：

```text
assets/papers/
```

建议文件名使用英文小写、数字和短横线，例如：

```text
2026-basic-research-high-quality-patents.pdf
```

然后在论文数据里写：

```js
href: "assets/papers/2026-basic-research-high-quality-patents.pdf"
```

注意网页路径使用 `/`，不要使用 Windows 的 `\`。

### 11. 替换论文配图

论文配图放在：

```text
assets/paper-thumbs/
```

默认规则是：论文 PDF 文件名和配图文件名保持同名，只改后缀。

```text
assets/papers/2026-basic-research-high-quality-patents.pdf
assets/paper-thumbs/2026-basic-research-high-quality-patents.jpg
```

如果 PDF 名和 JPG 名匹配，页面会自动找到配图。建议配图使用横向图片，能体现论文主题、研究对象、框架图或核心结果图。

### 12. 修改研究札记

打开 `script.js`，修改：

```js
data.zh.posts
data.en.posts
```

当前札记区主要作为入口和占位。以后如果要写正式博客，可以再新增独立文章页面或接入 Markdown 文章目录。

### 13. 调整动态背景

动态背景由两部分控制：

```css
styles.css 中的 #constellation
```

和：

```js
script.js 中的 initConstellation()
```

如果觉得背景太明显，优先调低：

```css
#constellation {
  opacity: 0.58;
}
```

如果想减少运动感，可以在 `initConstellation()` 中降低粒子数量、符号数量或动画速度。主页定位是“极简 + 科技感”，建议动效保持克制，不要压过学术内容。

### 14. 调整侧边栏导航

侧边栏锚点主要在 `index.html`，导航文字在：

```js
content.zh.nav
content.en.nav
```

如果新增一个大 section，需要同时做三件事：

1. 在 `index.html` 添加对应区块，并设置 `id`。
2. 在侧边栏和移动端导航里添加 `href="#对应id"`。
3. 在 `script.js` 的 `content.zh.nav` 和 `content.en.nav` 中补充中英文文案。

## 四、发布到 GitHub Pages

确认本地预览没有问题后，执行：

```powershell
cd D:\codex_page
git status
git add index.html script.js styles.css assets/ 维护说明书.md README.md
git commit -m "Update homepage content"
git push origin main
```

推送后打开：

```text
https://lyncaizhang.github.io/
```

如果线上页面仍是旧版本，可以在网址后加一个刷新参数：

```text
https://lyncaizhang.github.io/?v=20260619
```

## 五、每次维护前后的检查清单

维护前：

1. 明确这次只改哪类材料：头像、邮箱、论文、动态、经历、研究兴趣或样式。
2. 准备好中英文版本，尤其是论文题名、简介和动态。
3. 图片和 PDF 文件名尽量使用英文、数字和短横线。

维护后：

1. 本地打开中文页面，检查中文内容是否自然。
2. 切换英文页面，检查英文内容是否同步。
3. 点击侧边栏导航，确认能跳到对应区块。
4. 点击邮箱按钮，确认能展示邮箱并复制。
5. 检查论文 PDF 链接和论文配图是否能正常打开。
6. 移动端缩小浏览器窗口，确认内容没有重叠。
7. `git status` 查看哪些文件被修改，再提交和推送。

## 六、常见问题

### 页面切换英文后内容还是中文

通常是只修改了 `data.zh` 或 `content.zh`，没有同步修改 `data.en` 或 `content.en`。

### 论文配图不显示

优先检查三件事：

1. 配图是否放在 `assets/paper-thumbs/`。
2. 配图文件名是否和 PDF 文件名一致。
3. 后缀是否是 `.jpg`，大小写是否一致。

### 页面线上没更新

可能是 GitHub Pages 部署延迟或浏览器缓存。先等 1-3 分钟，再强制刷新；仍不更新时，在网址后加 `?v=日期`。

### 修改代码后页面空白

大概率是 `script.js` 中少了逗号、引号或括号。优先检查最近修改的数组对象，尤其是 `news`、`status`、`topics`、`publications`。

## 七、推荐维护节奏

平时小更新可以采用：

```text
准备材料 -> 修改 script.js/assets -> 本地预览 -> 提交 -> 推送 -> 线上检查
```

建议每次只做一个主题的维护。例如本次只加论文，下次再调样式。这样如果线上出现问题，也更容易定位原因。
